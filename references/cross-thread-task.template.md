# 模块间转派提示词

请负责 `<确实需要你承担的独立模块范围>`，完成 `<目标和可观察的验收结果>`。
必要上下文：`<文件、输入或链接>`。已有授权：`<实施与交付路径>`。
实际限制：`<明确存在的边界；没有则删除，不自行添加禁止项>`。
本次唯一回传任务 ID：`<当前请求来源的真实 ID>`。完成、失败或需要决定时，
用 send_message_to_thread 向该 ID 实际发送一次结果或阻断；自己的 final 不算回传，
不沿用历史 Main/lead，不回复“收到”或多方抄送。
我继续处理 `<独立范围；没有则删除>`，并负责原生等待结果和后续授权交付。

归属有误时说明实际负责人；确需再次转派时保留本次唯一回传 ID，并说明谁整合。
不要重复转回、广播或形成多个协调者。小范围工作不必转派，无需额外编号或回传文件。
